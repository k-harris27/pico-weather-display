# Weather Display for Inky Frame 7.3 Spectra

To enable code completion with e.g. pylance, install the stubs listed in `requirements.txt` into ./typings.

```bash
python3 -m pip install -r ./requirements.txt --target ./typings/ --no-user
```